def pairSum(data,goal):
    i=0
    while i in range(len(data)):
        for i!=goal:
            j=data[i]+data[i+1]
            if j!=goal:
                response=False
                else:
                    response=True
    return response
